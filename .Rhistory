setwd("~/dacss601/demo3")
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Maternal%20mortality%20ratio.csv"
dirty=read.csv(linkInfantMortalityRate)
dirty=read.csv(linkInfantMortalityRate)
str(dirty)
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
str(dirtyIMR)
str(dirtyLEAB)
str(dirtyMMR)
head(dirtyIMR) # the variable here is called "X000.live.births" and its an int
head(dirtyLEAB)# the variable here is called "years" and its a num
head(dirtyMMR) # the variable here is called "deaths.100" and its an int
str(dirtyIMR) # the variable here is called "deaths." and its an double
head(dirtyIMR)
str(dirtyIMR) # the variable here is called "deaths." and its an double
# looking at the top to see if its all good
head(dirtyIMR)
# looking at the bottom to see if its all good
tail(dirtyIMR)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
# looking at the top and we see its fine other than the offset column name
head(dirtyIMR)
# looking at the bottom and we see its fine
tail(dirtyIMR)
names(dirtyIMR)
str(dirtyLEAB)# the variable here is called "years" and its a num
head(dirtyLEAB)# the variable here is called "years" and its a num
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
head(dirtyMMR) # the variable here is called "deaths.100" and its an int
#grab only the columns we want to keep
dirtyIMR<-df %>%
select(name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
#grab only the columns we want to keep
dirtyIMR<-df %>% dplyr::select(
name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
#grab only the columns we want to keep
libary(dplyr)
#grab only the columns we want to keep
library(dplyr)
install.packages("dplyr")
#grab only the columns we want to keep
library(dplyr)
dirtyIMR<-df %>% select(
name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
#grab only the columns we want to keep
library(dplyr)
dirtyIMR <- df %>% select(name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
# looking at the top and we see its fine other than the offset column name
head(dirtyIMR)
# looking at the bottom and we see its fine
tail(dirtyIMR)
names(dirtyIMR) # "deaths.1" and "X000.live.births" should be the same name
#grab only the columns we want to keep
library(dplyr)
cleanIMR <- df %>%
select(name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
names(dirtyIMR) # "deaths.1" and "X000.live.births" should be the same name
#grab only the columns we want to keep
library(dplyr)
cleanIMR <- df %>%
select(name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
#grab only the columns we want to keep
library(dplyr)
cleanIMR <- dirtyIMR %>%
select(name = countryname,
deaths.1 = deaths.1000.live.births,
ranking = region)
#grab only the columns we want to keep
library(dplyr)
cleanIMR <- dirtyIMR %>%
select(countryname = countryname,
deaths.1000.live.births = deaths.1,
region = ranking)
#grab only the columns we want to keep
library(dplyr)
cleanIMR <- dirtyIMR %>%
select(countryname = name,
deaths.1000.live.births = deaths.1,
region = ranking)
names(cleanIMR)
#grab only the columns we want to keep
library(dplyr) #
cleanIMR <- dirtyIMR %>%
select(countryname = name,
deaths.1000.live.births = deaths.1,
region = ranking)
names(cleanIMR)
str(dirtyIMR)
#grab only the columns we want to keep
library(dplyr) #
cleanIMR <- dirtyIMR %>%
select(countryname = name,
deaths.1000.live.births = deaths.1,
region = ranking)
names(cleanIMR)
str(cleanIMR)
#grab only the columns we want to keep
library(dplyr) # bring in the library
cleanIMR <- dirtyIMR %>%
select(countryname = slug,
deaths.1000.live.births = deaths.1,
region = ranking) # take the 3 coulmns we want to keep and change the title to match what we want
names(cleanIMR) # checking the names of the columns
str(cleanIMR) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
head(dirtyLEAB)# the variable here is called "years" and its a num
cleanLEAB <- dirtyLEAB %>%
select(countryname = slug,
years = years,
region = region) # take the 3 columns we want to keep and change the title to match what we want
names(cleanLEAB) # checking the names of the columns
str(cleanLEAB) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
str(dirtyLEAB)# the variable here is called "years" and its a num
str(dirtyLEAB)# the variable here is called "years" and its a num
# take the 3 columns we want to keep and change the title to match what we want
cleanLEAB <- dirtyLEAB %>% select(countryname = slug,
years = years,
region = region)
str(cleanLEAB) # checking the data types of the columns
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
# we see that the data column also got split into 2
# take the 3 columns we want to keep and change the title to match what we want
cleanMMR <- dirtyMMR %>% select(countryname = slug,
deaths.100000.live.births = deaths.100,
region = ranking)
str(cleanMMR) # checking the data types of the columns
saveRDS(cleanIMR, file = "cleanMMR.rds")
write.csv(cleanIMR, file = "cleanMMR.csv", row.names = FALSE)
saveRDS(cleanIMR, file = "cleanMMR.rds") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.rds") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.rds") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
saveRDS(cleanIMR, file = "cleanIMR.rds") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.rds") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.rds") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
#grab only the columns we want to keep from the IMR data
library(dplyr) # bring in the library
# take the 3 columns we want to keep and change the title to match what we want
cleanIMR <- dirtyIMR %>% select(countryname = slug,
deaths.1000.live.births = deaths.1,
region = ranking)
str(cleanIMR) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
# take the 3 columns we want to keep and change the title to match what we want
cleanLEAB <- dirtyLEAB %>% select(countryname = slug,
years = years,
region = region)
str(cleanLEAB) # checking the data types of the columns
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
# we see that the data column also got split into 2
# take the 3 columns we want to keep and change the title to match what we want
cleanMMR <- dirtyMMR %>% select(countryname = slug,
deaths.100000.live.births = deaths.100,
region = ranking)
str(cleanMMR) # checking the data types of the columns
saveRDS(cleanIMR, file = "cleanIMR.rds") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.rds") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.rds") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
setwd("~/dacss601/demo4")
setwd("~/dacss601/demo3")
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
#grab only the columns we want to keep from the IMR data
library(dplyr) # bring in the library
# take the 3 columns we want to keep and change the title to match what we want
cleanIMR <- dirtyIMR %>% select(countryname = slug,
deaths.1000.live.births = deaths.1,
region = ranking)
str(cleanIMR) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
# take the 3 columns we want to keep and change the title to match what we want
cleanLEAB <- dirtyLEAB %>% select(countryname = slug,
years = years,
region = region)
str(cleanLEAB) # checking the data types of the columns
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
# we see that the data column also got split into 2
# take the 3 columns we want to keep and change the title to match what we want
cleanMMR <- dirtyMMR %>% select(countryname = slug,
deaths.100000.live.births = deaths.100,
region = ranking)
str(cleanMMR) # checking the data types of the columns
saveRDS(cleanIMR, file = "cleanIMR.RDS") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.RDS") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.RDS") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
str(readRDS("cleanIMR.RDS"))
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
#grab only the columns we want to keep from the IMR data
library(dplyr) # bring in the library
# take the 3 columns we want to keep and change the title to match what we want
cleanIMR <- dirtyIMR %>% select(countryname = slug,
deaths.1000.live.births = deaths.1,
region = ranking)
str(cleanIMR) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
# take the 3 columns we want to keep and change the title to match what we want
cleanLEAB <- dirtyLEAB %>% select(countryname = slug,
years = years,
region = region)
str(cleanLEAB) # checking the data types of the columns
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
# we see that the data column also got split into 2
# take the 3 columns we want to keep and change the title to match what we want
cleanMMR <- dirtyMMR %>% select(countryname = slug,
deaths.100000.live.births = deaths.100,
region = ranking)
str(cleanMMR) # checking the data types of the columns
saveRDS(cleanIMR, file = "cleanIMR.RDS") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.RDS") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.RDS") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
str(readRDS("cleanIMR.RDS"))
str(readRDS("cleanIMR.RDS"))
str(read.csv("cleanIMR.csv"))
saveRDS(cleanIMR, file = "cleanIMR.RDS") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.RDS") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.RDS") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
str(readRDS("cleanIMR.RDS"))
str(read.csv("cleanIMR.csv"))
str(readRDS("cleanLEAB.RDS")) # read the LEAB data RDS version
str(read.csv("cleanLEAB.csv"))  # read the LEAB data CSV version
str(readRDS("cleanMMR.RDS")) # read the MMR data RDS version
str(read.csv("cleanMMR.csv"))  # read the MMR data CSV version
# github link to "Infant mortality rate.csv"
linkInfantMortalityRate="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Infant%20mortality%20rate.csv"
# github link to "Life expectancy at birth.csv"
linkLifeExpectancyAtBirth="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Life%20expectancy%20at%20birth.csv"
# github link to "Maternal mortality ratio.csv"
linkMaternalMortalityRatio="https://github.com/FuntamendalsRedwan/demo3/raw/refs/heads/main/Original%20data/Maternal%20mortality%20ratio.csv"
# store the Infant mortality rate into IMR
dirtyIMR=read.csv(linkInfantMortalityRate)
# store the Life Expectancy at Birth into LEAB
dirtyLEAB=read.csv(linkLifeExpectancyAtBirth)
# store the Matrenal Mortality Ratio into MMR
dirtyMMR=read.csv(linkMaternalMortalityRatio)
# we see that one of the column name got split into 2: "deaths.1" and "x000.live.births"
# and so the last few columns are offset
str(dirtyIMR)
#grab only the columns we want to keep from the IMR data
library(dplyr) # bring in the library
# take the 3 columns we want to keep and change the title to match what we want
cleanIMR <- dirtyIMR %>% select(countryname = slug,
deaths.1000.live.births = deaths.1,
region = ranking)
str(cleanIMR) # checking the data types of the columns
str(dirtyLEAB)# the variable here is called "years" and its a num
# take the 3 columns we want to keep and change the title to match what we want
cleanLEAB <- dirtyLEAB %>% select(countryname = slug,
years = years,
region = region)
str(cleanLEAB) # checking the data types of the columns
str(dirtyMMR) # the variable here is called "deaths.100" and its an int
# we see that the data column also got split into 2
# take the 3 columns we want to keep and change the title to match what we want
cleanMMR <- dirtyMMR %>% select(countryname = slug,
deaths.100000.live.births = deaths.100,
region = ranking)
str(cleanMMR) # checking the data types of the columns
saveRDS(cleanIMR, file = "cleanIMR.RDS") # save cleanIMR as an rds
write.csv(cleanIMR, file = "cleanIMR.csv", row.names = FALSE)  # save cleanIMR as a csv
saveRDS(cleanLEAB, file = "cleanLEAB.RDS") # save cleanLEAB as an rds
write.csv(cleanLEAB, file = "cleanLEAB.csv", row.names = FALSE)  # save cleanLEAB as a csv
saveRDS(cleanMMR, file = "cleanMMR.RDS") # save cleanMMR as an rds
write.csv(cleanMMR, file = "cleanMMR.csv", row.names = FALSE)  # save cleanMMR as a csv
str(readRDS("cleanIMR.RDS")) # read the IMR data RDS version
str(read.csv("cleanIMR.csv"))  # read the IMR data CSV version
str(readRDS("cleanLEAB.RDS")) # read the LEAB data RDS version
str(read.csv("cleanLEAB.csv"))  # read the LEAB data CSV version
str(readRDS("cleanMMR.RDS")) # read the MMR data RDS version
str(read.csv("cleanMMR.csv"))  # read the MMR data CSV version
setwd("~/dacss601/demo4")
